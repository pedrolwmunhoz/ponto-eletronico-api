springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: ${PORT:8081}

spring:
  application:
    name: ponto-eletronico-api

  datasource:
    # Se as env vars não estiverem definidas (ex.: local), usa os valores abaixo. No Render, defina no Dashboard.
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&sslmode=require&options=-c%20lock_timeout%3D20000%20-c%20idle_in_transaction_session_timeout%3D30000}
    username: ${SPRING_DATASOURCE_USERNAME:postgres.tnlgxtcyfurrutpimbbs}
    password: ${SPRING_DATASOURCE_PASSWORD:atheCobol4ever.@1}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: none

jwt:
  public.key: classpath:keys/app.pub
  private.key: classpath:keys/app.key

app:
  jwt:
    expiration-ms: 900000
    refresh-expiration-ms: 86400000
  brute-force:
    max-tentativas: 5
    janela-minutos: 15
