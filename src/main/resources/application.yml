springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: 8081

spring:
  application:
    name: ponto-eletronico-api

  datasource:
    url: jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&sslmode=require
    username: postgres.tnlgxtcyfurrutpimbbs
    password: atheCobol4ever.@1
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: always
      schema-locations: classpath:schema-feriado.sql

jwt:
  public.key: classpath:keys/app.pub
  private.key: classpath:keys/app.key

app:
  jwt:
    expiration-ms: 900000
    refresh-expiration-ms: 86400000
  brute-force:
    max-tentativas: 5
    janela-minutos: 15
